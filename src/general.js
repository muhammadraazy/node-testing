export const summarize = (one, two) => {
    return one + two
}

export const multiply = (one, second) => {
    return one * second
}

export const truthy = (value) => {
    return value;
}

export const numberTest = (num) => {
    return num;
}

export const asynchronousFunc = async(myName = "mohammed") => {
    return myName + "raazy"
}